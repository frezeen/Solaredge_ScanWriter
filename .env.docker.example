# SolarEdge ScanWriter - Docker Environment Configuration
# Copia questo file in .env e modifica con i tuoi valori

# =============================================================================
# CREDENZIALI SOLAREDGE (OBBLIGATORIE)
# =============================================================================
SOLAREDGE_SITE_ID=123456
SOLAREDGE_USERNAME=your.email@example.com
SOLAREDGE_PASSWORD=your_password
SOLAREDGE_API_KEY=your_api_key

# =============================================================================
# CONFIGURAZIONE INFLUXDB
# =============================================================================
INFLUXDB_USERNAME=admin
INFLUXDB_PASSWORD=solaredge123
INFLUXDB_ORG=fotovoltaico
INFLUXDB_BUCKET=solaredge
INFLUXDB_TOKEN=solaredge-super-secret-token-2024

# =============================================================================
# CONFIGURAZIONE GRAFANA
# =============================================================================
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin

# =============================================================================
# CONFIGURAZIONE SISTEMA
# =============================================================================
TZ=Europe/Rome
LOG_LEVEL=INFO

# =============================================================================
# CONFIGURAZIONE MODBUS (OPZIONALE)
# =============================================================================
# Decommentare e configurare solo se hai l'inverter in rete
# REALTIME_MODBUS_HOST=192.168.1.100
# REALTIME_MODBUS_PORT=1502

# =============================================================================
# CONFIGURAZIONE AVANZATA (OPZIONALE)
# =============================================================================
# Timeout e intervalli (in secondi)
GLOBAL_TIMEOUT_SECONDS=30
SCHEDULER_API_DELAY_SECONDS=1
SCHEDULER_REALTIME_DELAY_SECONDS=5

# Porte servizi (modificare solo se necessario)
GUI_PORT=8092
INFLUXDB_PORT=8086
GRAFANA_PORT=3000

# Cache TTL (in secondi)
CACHE_TTL_SECONDS=900